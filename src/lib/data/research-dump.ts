import type { ResearchDumpEntry } from '$lib/types';

// Inbox for raw notes, links, quotes, PDFs, and ideas that are distilled
// enough to show in-app but not yet promoted into `restaurants.ts`.
export const researchDump: ResearchDumpEntry[] = [];
