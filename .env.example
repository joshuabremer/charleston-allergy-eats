# Copy this file to .env and fill in real values. .env is gitignored.

# Google Geocoding API key, used by scripts/geocode.ts. Create one at
# https://console.cloud.google.com/google/maps-apis/credentials with the
# "Geocoding API" enabled, and (recommended) restrict it to that API.
GOOGLE_MAPS_API_KEY=
